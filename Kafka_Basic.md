## Each broker contains:
```Code
Kafka Broker
 ├── Topic Partitions (stored on disk)
 ├── Replica Manager
 ├── Log Manager
 ├── Network Layer
 ├── Controller (if elected)
 ├── Group Coordinator
 └── Request Handler Threads
```
